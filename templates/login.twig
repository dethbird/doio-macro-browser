<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title|default('Login') }}</title>
  <style>
    :root { color-scheme: light dark; }
    html, body { height: 100%; }
    body {
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      margin: 0; padding: 0; display: grid; place-items: center;
      background: Canvas; color: CanvasText;
    }
    .card { width: 100%; max-width: 360px; border: 1px solid; border-color: color-mix(in srgb, CanvasText 12%, transparent);
      border-radius: 10px; padding: 20px; background: color-mix(in srgb, Canvas 96%, CanvasText 4%);
      box-shadow: 0 2px 16px rgba(0,0,0,.08);
    }
    h1 { margin: 0 0 10px 0; font-size: 20px; }
    label { display: grid; gap: 6px; font-size: 12px; color: color-mix(in srgb, CanvasText 70%, transparent); }
    input[type="password"] { border: 1px solid; border-color: color-mix(in srgb, CanvasText 20%, transparent); border-radius: 8px; padding: 8px 10px; font-size: 14px; background: Canvas; color: CanvasText; }
    button { border: 1px solid; border-color: color-mix(in srgb, CanvasText 20%, transparent); border-radius: 8px; padding: 8px 12px; background: color-mix(in srgb, Canvas 92%, CanvasText 8%); color: CanvasText; cursor: pointer; }
    button:hover { background: color-mix(in srgb, Canvas 86%, CanvasText 14%); }
    .row { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
    .error { color: #b91c1c; font-size: 13px; margin-top: 6px; }
    .hint { font-size: 12px; color: color-mix(in srgb, CanvasText 60%, transparent); margin-top: 12px; }
  </style>
</head>
<body>
  <form class="card" method="post" action="/login">
    <h1>Protected</h1>
    <p style="margin:0 0 12px 0; font-size:14px;">Enter the password to access this site.</p>
    <label>
      <span>Password</span>
      <input type="password" name="password" autofocus required />
    </label>
    {% if error %}
      <div class="error">{{ error }}</div>
    {% endif %}
    <div class="row" style="margin-top:12px;">
      <div></div>
      <button type="submit">Sign in</button>
    </div>
    <div class="hint">Configured via APP_PASSWORD in .env</div>
  </form>
</body>
</html>
