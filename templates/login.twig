<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title|default('Login') }}</title>
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="shortcut icon" href="/favicon.ico" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.2/css/bulma.min.css">
  <style>
    html, body { height: 100%; }
    body { display: grid; place-items: center; }
  </style>
</head>
<body>
  <form class="box" style="width:100%; max-width: 420px;" method="post" action="/login">
    <h1 class="title is-4">Protected</h1>
    <p class="content is-small" style="margin-top:-0.5rem;">Enter the password to access this site.</p>
    <div class="field">
      <label class="label is-small">Password</label>
      <div class="control">
        <input class="input" type="password" name="password" autofocus required />
      </div>
      {% if error %}
        <p class="help is-danger">{{ error }}</p>
      {% endif %}
    </div>
    <div class="field is-grouped is-justify-content-flex-end">
      <div class="control">
        <button type="submit" class="button is-primary">Sign in</button>
      </div>
    </div>
    <p class="help">Configured via APP_PASSWORD in .env</p>
  </form>
</body>
</html>
