<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title|default('DOIO Macro Browser') }}</title>
  <style>
    html, body { height: 100%; margin: 0; }
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Helvetica, Arial, sans-serif; }
    #root { min-height: 100%; }
  </style>
  {# Optional: when Vite build outputs CSS, include it here using a manifest-aware helper in the future #}
  {# {% if css %}<link rel="stylesheet" href="{{ css }}">{% endif %} #}
</head>
<body>
  <div id="root">Loading…</div>

  {# Placeholder for built JS; in dev we'll use Vite server, in prod we'll read manifest.json #}
  {# {% if js %}<script type="module" src="{{ js }}"></script>{% endif %} #}
</body>
</html>
